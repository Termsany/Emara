kind = "web"
previewPath = "/"
title = "EMARA | عمارة"
version = "1.0.0"
id = "artifacts/emara"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20740

[services.development]
run = "pnpm --filter @workspace/emara run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/emara", "run", "build" ]
publicDir = "artifacts/emara/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20740"
BASE_PATH = "/"
